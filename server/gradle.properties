# Gradle properties for Quiz API
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true

# Java compilation parameters
# Force compiler to preserve parameter names for reflection (required for Spring MVC @PathVariable)
org.gradle.jvmargs=-Dfile.encoding=UTF-8
